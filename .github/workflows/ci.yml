name: SAYMERA CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Use Node.js 24
        uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Validate SAYMERA
        run: npm test
  pattern-browser:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium webkit
      - name: Verify number-pattern phone and tablet interactions
        run: node tests/pattern-browser.test.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: number-pattern-browser-screenshots
          path: test-results/
  odd-even-browser:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium webkit
      - name: Verify odd-even phone and tablet interactions
        run: node tests/odd-even-browser.test.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: odd-even-browser-screenshots
          path: odd-even-test-results/
  preschool-match-browser:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium webkit
      - name: Verify NEL preschool matching phone and tablet interactions
        run: node tests/preschool-match-browser.test.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: preschool-match-browser-screenshots
          path: preschool-test-results/
  preschool-sort-browser:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium webkit
      - name: Verify NEL preschool sorting phone and tablet interactions
        run: node tests/preschool-sort-browser.test.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: preschool-sort-browser-screenshots
          path: preschool-sort-test-results/

  preschool-compare-browser:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium webkit
      - name: Verify NEL preschool comparing phone and tablet interactions
        run: node tests/preschool-compare-browser.test.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: preschool-compare-browser-screenshots
          path: preschool-compare-test-results/

  preschool-order-browser:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium webkit
      - name: Verify NEL preschool ordering phone and tablet interactions
        run: node tests/preschool-order-browser.test.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: preschool-order-browser-screenshots
          path: preschool-order-test-results/

  preschool-pattern-browser:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium webkit
      - name: Verify NEL preschool patterning phone and tablet interactions
        run: node tests/preschool-pattern-browser.test.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: preschool-pattern-browser-screenshots
          path: preschool-pattern-test-results/

  preschool-rote-browser:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium webkit
      - name: Verify NEL preschool rote counting phone and tablet interactions
        run: node tests/preschool-rote-browser.test.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: preschool-rote-browser-screenshots
          path: preschool-rote-test-results/

  preschool-reliable-browser:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium webkit
      - name: Verify NEL preschool reliable counting phone and tablet interactions
        run: node tests/preschool-reliable-browser.test.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: preschool-reliable-browser-screenshots
          path: preschool-reliable-test-results/

  preschool-subitise-browser:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium webkit
      - name: Verify NEL preschool subitising phone and tablet interactions
        run: node tests/preschool-subitise-browser.test.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: preschool-subitise-browser-screenshots
          path: preschool-subitise-test-results/

  preschool-conservation-browser:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium webkit
      - name: Verify NEL preschool conservation phone and tablet interactions
        run: node tests/preschool-conservation-browser.test.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: preschool-conservation-browser-screenshots
          path: preschool-conservation-test-results/

  preschool-number-representations-browser:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium webkit
      - name: Verify NEL preschool number-representation phone and tablet interactions
        run: node tests/preschool-number-representations-browser.test.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: preschool-number-representations-browser-screenshots
          path: preschool-number-representations-test-results/


  preschool-numeral-formation-browser:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium webkit
      - name: Verify NEL preschool numeral-formation phone and tablet interactions
        run: node tests/preschool-numeral-formation-browser.test.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: preschool-numeral-formation-browser-screenshots
          path: preschool-numeral-formation-test-results/

  preschool-quantity-comparison-browser:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium webkit
      - name: Verify NEL preschool quantity-comparison phone and tablet interactions
        run: node tests/preschool-quantity-comparison-browser.test.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: preschool-quantity-comparison-browser-screenshots
          path: preschool-quantity-comparison-test-results/


  preschool-part-whole-browser:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium webkit
      - name: Verify NEL preschool part-whole phone and tablet interactions
        run: node tests/preschool-part-whole-browser.test.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: preschool-part-whole-browser-screenshots
          path: preschool-part-whole-test-results/
